version: "3"
services:
  node-app:
    image: node-app-image
    container_name: node-app
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/dist
    environment:
      - PORT=3000
    env_file:
      - ./.env